import {
  truncateProperty
} from '../../../helpers/truncate-property';
import { module, test } from 'qunit';

module('TruncatePropertyHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = truncateProperty(42);
  assert.ok(result);
});
